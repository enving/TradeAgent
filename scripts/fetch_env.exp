#!/usr/bin/expect -f
set timeout 20
spawn scp -o StrictHostKeyChecking=no recovery@raspberrypi.local:~/tradeagent/.env .env_from_pi
expect {
  "password:" {
    send "raspberry\r"
    exp_continue
  }
  eof
}

wait
