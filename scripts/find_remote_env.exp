#!/usr/bin/expect -f
set timeout 20
spawn ssh -o StrictHostKeyChecking=no recovery@raspberrypi.local "find . -maxdepth 3 -name .env"
expect {
  "password:" {
    send "raspberry\r"
    exp_continue
  }
  eof
}

wait
